const div = document.createElement('div')
div.innerText = "Olá, Devs!"

const body = document.querySelector('body')
body.prepend(div)

// aprrend (adiciona algo depois do elemento)

// prepend (adiciona algo antes de todos os elemtnso)